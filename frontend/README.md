# Majelani Accounting — Frontend
Hosted on Cloudflare Pages.

Build settings:
- Root directory: `frontend`
- Output directory: `/` (static export)

All frontend source code must stay inside the `frontend` directory.